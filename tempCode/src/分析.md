#扩展容易
以下内容会很多种，目的扩展一种，对代码改动集中，模块划分清晰。
## 技能
	技能属性，action对象
## buff
	加新 state， 每秒产生的action对象

# 跨服切换场景
## 我的设计
	设定环境，多服务器多场景，网络稳定。宕机或者网络断开重连不考虑。全部重启解决。
## world player
	state: 
		Playing, //收到scene player请求进入Moving --》 Moving, 响应ok.
				//包含在那个sceneId  信息供查询。

		Moving, //传送前申请，目标场景服务器 预定位置，失败,成功 --》 通知scene player
				//成功后，接受scene 之间的移动转发，或者，scene 直接沟通，不转发
				// 缓存 新登录。 缓存新查询，等请求消息。
## acc Player
	state: 
		Playing, //收到scene player请求进入Moving --》 Moving, 响应ok.
				 //转发 消息
		Moving, //收到world player请求进入Playing --》 Playing
				//缓存消息，不发。

## scene Player
	state: 
		Playing, //请求world进入moving,接收world响应ok
				 //请求acc进入moving,接收acc响应ok
				 //acc, world都ok,--》Moving.  保证Moving后，不会收到 Player 的业务请求

		Moving, //申请预定位置
				//case 失败 --》 Playing.  通知world, acc转换到Playing
				//case 成功-->移动到目标--》删除自己
				
		
				
## 目标scene Player 
	state: 
		None,
				//case 有预定，成功创建Player,预定状态
				//case 人满，返回预定失败。
		Reserve , //Playeing come in, -->Playing, 通知world转换到Playing
				//预定成功就必须有Player移动进来，没有别的情况。除非宕机。
				 //Reserve state用 vector<Reserve Player > 实现
				 
		Moving	 //刚移动过来的player,是 Moving 状态
				 //通知world, acc转换到Playing，同时通知sceneId信息
		Playing,